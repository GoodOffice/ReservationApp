// ReservationApp.h : Include file for standard system include files,
// or project specific include files.

#pragma once

#ifndef ReservationApp_
#define ReservationApp_

#include "Vehicle.h"
bool toggleView(int flag) {
	if (flag == 1)
		return true;
	else
		return false;

}

#endif